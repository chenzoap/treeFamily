rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Reglas DEV para emulador local.
    // Permiten lectura/escritura libre solo en desarrollo.
    // NO usar este archivo para producción.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}